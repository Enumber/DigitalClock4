[English](MODIFICATIONS.md) · **中文**

# 修改说明（Linux 维护版）

本仓库是 [Digital Clock 4](https://github.com/Kolcha/DigitalClock4)（作者 Nick Korotysh，
GPL-3.0-or-later）的 **Linux 向维护版**。上游曾支持 Linux，但从未提供安装包；4.7.9
（2020-12）之后项目已归档。本仓库让程序能在 Linux 上编译、安装与日常使用。

**本维护版维护者：** **ENum**（GitHub: [Enumber](https://github.com/Enumber)）。

修改内容仍以 GPL-3.0-or-later 许可；原版权声明保留。

## 构建修复
- 空格路径下 `lrelease`、英文 `.ts` 桩、`<QWidget>` 头文件、QHotkey qmake 与第三方入库。

## 翻译
- 修复插件翻译资源加载；补全简体中文；修正快捷键与误译。语言仍跟随系统 locale。

## 与官方 Windows 版对齐
- 正确应用图标；附带皮肤/纹理；关机插件；朗读时钟；X11 全屏检测；任务栏图标开关。

## 缺陷修复与打包
- 自启动路径修正；**移除**自动更新联网检查（主程序自此不再主动联网；IP 地址
  插件仍是可选功能，默认关闭）；`$ORIGIN` RUNPATH；交互式 `install.sh`；
  补全构建依赖文档（含 `libxi-dev`）。

## 安装器（2026-07-23 至 2026-07-29）

- 新增图形化安装/卸载对话框（GTK），并把它写成安装器的主要使用方式；原来的
  纯终端问答流程改成没有显示器时的回退方案。
- 程序目录和配置目录分开；卸载默认两个一起删，除非另外指定。
- 修正原地安装（inplace）时桌面图标路径和显示名称不对的问题。
- 安装器不再在安装过程中询问是否开机自启——现在唯一能控制这个开关的地方是
  应用自己设置里的选项。
- 加固图形界面的路径解析：不再对自由文本路径做 `eval`。
- 图标转换改用 `mktemp`，不再用可预测的 `/tmp` 文件名。
- 图形结果对话框只在交互式运行时弹出；顺手删掉一个过期的开发目录通配规则。
- 修了安装器自己"第二实例检测"里的一个自杀 bug，并新增了窗口拉起 + 陈旧锁
  回收——现在再开一次安装器会切到已经开着的那个窗口，而不是没反应或卡死。

## 设置与界面整理（2026-08-04）

- **新增"可覆盖面板"快捷开关**：托盘图标菜单和时钟窗口自己的右键菜单（紧跟在"位置"
  下面）都有一个可勾选项，跟设置对话框"杂项"标签下的同名开关双向同步，切换后立即
  生效并立即落盘，不需要打开设置对话框、也不需要重启程序。
- **原来"改了要重启"的选项现在全部改了立刻生效**：全屏检测、多工作空间显示、
  保持可见、点击移动、Linux 优化的"始终置顶"模式、在所有显示屏上显示时钟、任务栏
  图标显示，全都不再需要重启。原来的"实验性选项"标签——之所以存在正是因为这些
  选项当年要重启——现在整体删除；设置里已经没有任何选项还需要重启了（唯一剩下
  的例外"只允许一个实例"，见下一条，已经不再是用户可设置的选项）。
- **"只允许一个实例"改成强制行为，不再是可选项**——这个复选框已经删除；第二次
  启动会直接被挡下退出，已经在跑的那个不受影响，跟原来的默认行为一致。
- 删掉了一个"启用自动更新"的死开关——这个复选框早就被强制隐藏（因为自动更新
  联网检查功能本身已经整套删除），留着这个选项本身就跟"程序绝不主动联网"这个
  承诺自相矛盾。
- 托盘图标：Linux 下当桌面图标主题查找失败时（比如直接从源码构建树运行、没有
  安装到系统图标主题）的回退图标，之前是一张纯黑色线稿，现在换成正式的彩色
  应用图标。
- "关于"对话框：点击 logo 会循环切换到原作者藏的两张彩蛋插画，这个功能已经删除，
  logo 现在始终显示正式的彩色时钟图标。"捐赠"标签明确加了一句说明——这个比特币
  地址属于原作者 Nick Korotysh，捐赠不会经过、也不会被 ENum 收取。"链接"标签删掉
  了一个已经失效跳转到无关页面的死链接（`showroom.qt.io`），给上游 SourceForge
  的工单链接加了"已限制创建工单"的说明，并新增了一个指向本 fork 自己 GitHub
  仓库主页和 issue 列表的分组——之前这个对话框完全没有指向本仓库的入口。

## 设置对话框瘦身 + 修复菜单里多余的分隔线（2026-08-05）

- 托盘图标（以及时钟窗口自己）的右键菜单里，"关于"和"退出"之间连着有两条分隔线——
  这是上游"检查更新"菜单项（原本卡在两条线中间）被删除后留下的残留，视觉上像多了
  一道空行。重复的那条已删除。
- **删掉了"针对 Linux 更好的置顶"这个勾选框。** 它控制的是要不要在普通的
  `Qt::WindowStaysOnTopHint`（不是所有窗口管理器都真的会理会）之外，再叠加一层
  完全绕过窗口管理器的强化置顶。这个开关和普通的"始终置顶"选项本来默认就都是开着
  的，也就是说这个勾选框唯一的实际作用是让人**关掉**这层强化（代价是这个窗口会
  失去 alt-tab、不再被窗口管理器正常管理）——这是个不该暴露给用户的实现细节。
  现在 Linux 下"始终置顶"永远用这种更强硬的方式，没有什么可配置的了。
- **"外观"和"杂项"重新分组，仍然只有两个标签（加上"插件"）** ——这两个标签加起来
  塞了将近 35 项控件（不少是当年"实验性选项"标签被整体撤销时一股脑塞进来的），
  而且全是一坨平铺的勾选框，毫无章法。
  - **外观**：还是纯视觉——皮肤/纹理/背景色/上色、缩放、透明度、分隔符闪烁、
    时间格式、对齐方式、数字间距。内容没变，只是不再跟窗口行为类勾选框挤在一起。
  - **杂项**：不再是一坨平铺的勾选框，改成两个分组框——"窗口"（置顶、覆盖面板、
    多屏/多工作空间显示、全屏时可见、开机自启）和"鼠标与交互"（鼠标穿透、悬停
    效果、点击移动），后面跟着真正零散的小项：贴边吸附、刷新间隔、时钟URL跳转、
    显示/隐藏菜单项开关、显示另一时区、导出状态。
- **又删掉两个勾选框，跟上面删"针对 Linux 更好的置顶"是同一个道理**——它们唯一
  的实际作用都是让人打开一个没人会真的想要的行为：
  - "在任务栏（以及 Alt+Tab 切换窗口）中显示程序图标"——桌面时钟没理由出现在
    那里；这个选项本来默认就是关的，勾选框唯一能做的是让人把它**打开**。现在
    窗口在构造时就永远带上 `Qt::Tool` 标志，没什么可配置的了。
  - "防止时钟出现在屏幕内容以外的位置"——让时钟始终能被够到根本不是一个可选
    行为，就该是对的做法。`ClockWindow::CorrectPosition()` 现在永远做这个夹紧
    判断，不再受一个本来就默认为真的配置项控制。
- 主对话框保持作者原始设计的窗口尺寸（`.ui` 里声明的 613×447），不再按内容量
  自动收缩，视觉上不会显得比经典的设置窗口小一圈。
- **"外观"照着这个仓库第一个 git 提交重新搭建，连 `QGridLayout` 都还原了**——
  `git show` 这个 fork 最早一次提交才是唯一靠得住的"原始布局是什么样"的证据，
  因为这轮整理开始时，外观 tab 早就在更早一次会话里吸收了后来被拆掉的"实验性
  选项"tab 的内容。真正原始的外观只有：始终置顶、鼠标穿透、分隔符闪烁、
  不透明度、缩放、皮肤/字体选择、纹理/颜色/背景自定义——用一个 4 行网格布局
  排（第0行：勾选框列和不透明度/缩放滑块并排；第1行：皮肤占满；第2行：纹理
  自定义三格并排；第3行：背景颜色/改变颜色），不是这几轮一直在用的纵向堆叠
  `QVBoxLayout`。"鼠标经过时透明化"和"完全隐藏"本来就不属于外观，是从旧的
  实验性选项 tab 来的，现在挪去了杂项，紧挨着"点击移动"（三个都是"鼠标在
  时钟上的交互行为"）。换回网格布局后省掉了纵向堆叠多占的大半高度，但在这台
  Linux+中文字体环境下窗口还是比作者声明的 613×447 略高一些——大概率是 447
  当年是照着 Windows 字体量出来的，不是布局结构还能再抠的问题了。杂项还留着
  "高级设置..."按钮（长在杂项 tab 内部，不是全局按钮行），装覆盖面板、多屏/
  全屏显示、贴边吸附、刷新间隔、时钟URL、显示/隐藏菜单项开关、导出状态。

## 程序图标改回原作者的彩蛋插图（2026-08-05）

按明确要求，部分推翻上面"关于对话框整理"那次改动：点击 logo 循环切图这个
彩蛋**功能**依然保持删除状态，但两张彩蛋图里的一张——`images/if_clock-c_750020.png`
（从这个仓库的第一个提交里挖出来恢复的）——现在**只**用在两个地方：一是"关于"
对话框的 logo（`icons.qrc` 里专门起了个 `about-logo.png` 别名），二是装进应用
启动器的 `.desktop`/图标主题条目（`install.sh` 打包时还是用原来的文件名
`clock-icon.png`）。其余地方——任务栏/窗口图标、托盘图标兜底——仍然用跟
Windows 版 `.exe` 图标（`clock_icon_win.ico`）像素级一致的彩虹表盘图
（`images/clock_icon.png`，`app-icon.png` 别名，跟这轮彩蛋图之前完全一样没变）。
另一张恢复出来的图 `images/if_favorite_c_750016.png` 留在仓库里，暂时没接到
任何地方用。

## "覆盖面板"不再是个设置，行为改成默认永远开启（2026-08-05）

根据下面这条排查记录的结论——这个选项在 GNOME Shell 上不管怎么设都没用，因为
GNOME Shell 自己的顶栏永远合成在最上层——继续把它留作一个需要用户自己去找到
并打开的勾选框，纯粹是摩擦，在大多数人实际在用的桌面环境上换不来任何好处。
跟"针对 Linux 更好的置顶"、任务栏图标开关、"保持可见"是同一个判断标准：如果
关掉一个选项唯一的作用就是选择一个更差的默认行为，那这个选项本来就不该
暴露给用户。

- `OPT_ALLOW_OVER_PANELS` 已经从 `settings_keys.h` 和 `clock_settings.cpp` 里
  彻底删除——没有配置键，也没有默认值。
- `ClockWindow::UpdateBypassWindowManager()` 现在在 Linux 下无条件设置
  `Qt::X11BypassWindowManagerHint`；`UsableScreenRect()` 也永远把面板那条带
  算作可以占用的区域。在不会特殊对待自己面板的窗口管理器上（也就是下面
  记录的 GNOME Shell 那个特例之外的所有情况），时钟现在默认就能覆盖面板，
  没有什么可配置的了。
- 托盘图标右键菜单和时钟窗口自己右键菜单里的"覆盖面板"这一项都已经删除
  （`ContextMenu`/`TrayControl` 已经完全不再带这个动作和它的信号）。
- 高级设置对话框里对应的勾选框和说明文字也从"窗口"分组里删掉了；这个分组
  还留着"在所有显示屏上显示"和"全屏时显示"这两项——它们跟这个选项无关，
  没有动它们。
- 跑了一遍 `lupdate` 同步全部五份 `.ts` 文件，让删掉的字符串正确标成
  `vanished`，不会留下没人引用的孤立源文本。

## 排查"覆盖面板"：是 GNOME Shell 的限制，不是这边的 bug（2026-08-05）

用户反馈"覆盖面板"在真实桌面（GNOME Shell）上开了没反应，把
`ClockWindow::UpdateBypassWindowManager()` / `core::placement::usableRect()`
整个重新读了一遍——两处逻辑都是对的：开关打开后窗口确实拿到了
`Qt::X11BypassWindowManagerHint`，夹紧边界也确实把面板那条带算进了可用区域。
差距在外部：GNOME Shell 自己的顶栏是按设计合成在所有客户端窗口之上的，普通
应用能设的任何 X11 提示（override-redirect、always-on-top 或别的）都改变不了
这一点——这是个被广泛报告过的限制（比如 dash-to-panel 项目的
["Panel is above 'always on top' windows"](https://github.com/home-sweet-gnome/dash-to-panel/issues/838)
这条 issue，还有 [mutter#587](https://gitlab.gnome.org/GNOME/mutter/-/work_items/587)），
不是普通应用能从自己这边修的。拿一个装了假面板（strut窗口）的普通 X11 窗口
管理器重新核实过这个选项的逻辑——在那种环境下是正常生效的，问题具体就出在
"GNOME Shell 自己的顶栏没有任何应用能盖得住"。在绕过逻辑那行代码加了
注释，也把勾选框的提示文字和说明标签都补了一句大白话说明，省得以后又被
当成 bug 报一遍。

## 图标身份拆成两个独立的图标主题（2026-08-05）

上面那次把彩蛋插图恢复成应用列表图标的改动，副作用是所有原来共用同一个
`"digitalclock4"` hicolor 主题名的图标身份——应用菜单条目、桌面快捷方式、
托盘图标兜底——全都跟着一起变成了彩蛋图，因为它们本来共用一个主题名。
这不是本意：只有"应用列表"这一处身份该显示彩蛋图，桌面快捷方式和托盘图标
应该继续显示跟 Windows 版一致的彩虹表盘图。

现在拆成两个 hicolor 主题名，不再共用一个：
- `digitalclock4` —— 只给"应用菜单"（`applications/` 目录下的 `.desktop`）
  条目用，仍是彩蛋图 `if_clock-c_750020.png`。
- `digitalclock4-rainbow` —— 给桌面快捷方式和 Linux 下的托盘图标兜底用，
  指向 `clock_icon.png`——跟任务栏/窗口图标共用同一张彩虹表盘图，也是
  跟 Windows 版 `clock_icon_win.ico` 一致的那张。

"关于"对话框的 logo 不受这次拆分影响——它直接加载自己 `icons.qrc` 里的
`about-logo.png` 别名，压根不经过这两个主题名中的任何一个。

- `install.sh`：新增 `APP_ICON_RAINBOW` 变量（`clock_icon.png`，打包成
  `clock-icon-rainbow.png`），和原有的 `APP_ICON`（彩蛋图，`clock-icon.png`）
  并列存在。`install_theme_icon()`——原来是写死的一整块只装一份图标进
  `$APP_ID` 的逻辑——现在改成一个可复用函数，接收源文件和主题名两个参数，
  按身份各调用一次。`make_desktop()` 新增了图标覆盖参数，让桌面快捷方式
  的 `.desktop` 条目能指定用 `digitalclock4-rainbow`，而应用菜单条目继续
  用默认值。卸载逻辑同时删除两个主题图标（原有的 `remove_icon "$APP_ID"`
  和新增的 `remove_icon "$APP_ID-rainbow"`）。
- `digital_clock/gui/tray_control.cpp`：`QIcon::fromTheme("digitalclock4", ...)`
  改成 `QIcon::fromTheme("digitalclock4-rainbow", ...)`，让托盘图标（含它
  在源码构建树/主题查找失败时的回退图）始终是彩虹表盘图，不受应用菜单
  图标是什么样影响。

## "关于"对话框 logo 点击彩蛋重新加回，循环内容改成两张图（2026-08-05）

上游原本的行为：250ms 内连续点击"关于"对话框的 logo 三次
（`ClickableLabel::requiredClicksCount()`/`clickTimeout()`）会依次循环
三个状态——彩虹表盘（默认）→ 隐藏图 A → 隐藏图 B → 回到彩虹表盘。
这个 fork 更早一轮会话里把整个点击响应功能删掉过（logo 固定不动）。
这次按要求把点击响应加回来——但因为这个 fork 里"关于"logo 现在永久
显示彩蛋图而不是彩虹表盘（见上面两节），已经没有"彩虹表盘"这个状态可以
循环回去了。恢复后的循环只在两张彩蛋图之间来回切换
（`about-logo.png` ⇄ `about-logo-alt.png`），彩虹表盘不会再出现在这里。
触发方式没变：250ms 内连点三下。

- `digital_clock/resources/icons.qrc`：新增 `about-logo-alt.png` 别名，
  指向 `images/if_favorite_c_750016.png`（从这个仓库第一个提交里恢复出来
  的两张彩蛋图中的第二张；之前已经在仓库里但没接到任何地方用）。
- `digital_clock/gui/about_dialog.h`/`.cpp`：加回 `on_logo_lbl_clicked()`
  （Qt 按命名约定自动连接到 `logo_lbl` 的 `clicked()` 信号的槽函数），
  以及一个记录当前显示哪张图的 `logo_showing_alt_` 布尔状态。

## 修复一个 bug：浏览选时钟 URL 文件后其实没真正保存（2026-08-05）

既有 bug，"高级设置"对话框从 `settings_dialog.cpp` 拆出来的时候原样带过来的，
不是上面这几节改动引入的，是这轮最终复核时才发现的。
`on_browse_url_file_btn_clicked()` 选完文件后调用 `QLineEdit::setText()`
把路径显示出来，但 `setText()` 不会触发 `textEdited()`——而
`on_clock_url_edit_textEdited()` 唯一监听的、真正会把值落盘的信号正是这个。
也就是说点"..."按钮选文件只会更新文本框显示的内容，从来没真正保存过这个选项；
反而是手动在文本框里敲同样的路径才会生效。修复方式是在文件选择器返回后
显式 `emit OptionChanged(OPT_CLOCK_URL_STRING, ...)`，跟手动输入那条路径
的行为对齐。

## 安装器合并成单文件；这个 fork 有了自己的版本号（2026-08-05）

- **`install.sh` + `enum-gui-ask.py` 合并成一个文件。** 原来 GUI（Gtk3）代码
  在跟 `install.sh` 并排的 `enum-gui-ask.py` 里；现在直接以 heredoc 形式内嵌
  进 `install.sh`，每次要弹窗前才落进一个私有的 `mktemp -d` 临时目录，用完
  立刻删掉。顺手精简到只保留这个安装器真正会用到的四种对话框模式
  （install/uninstall/list/info）——共用模板里的多应用选择器模式在这里从
  没被用过（这个安装器只装这一个程序），没有当死代码继续带着，直接删掉了。
  行为没变；用真实的模拟鼠标点击（不是直接调控件 API，走完整 X11 事件路径）
  验证过。
- **安装器窗口/任务栏图标**现在跟 ENum Setup 自己的图标一致了（同一个
  `GLib.set_prgname("enum-setup")`，同一份 SVG 图标跟 GUI 代码一起内嵌），
  不再是通用的系统图标——视觉上这个安装器现在看起来就是 ENum Setup 家族的
  一部分。ENum Setup（或另一个 ENum 安装器）已经开着的时候再打开这个安装器，
  会直接切到那个已有窗口而不是弹出第二个——这靠的是两边本来就共用的单实例锁，
  这次改动没有动它，只是确认合并成单文件之后依然正确。
- **新增 `digital_clock/core/fork_version.h`**，`DIGITALCLOCK4_FORK_VERSION =
  "1.0"`——这个 fork 之前完全没有自己的版本号，唯一存在的版本字符串
  （`main.cpp` 里 `QApplication::setApplicationVersion("4.7.9")`）是上游的，
  停留在这个 fork 分出来那一刻的值，不会跟着这份文档记录的 fork 侧改动走。
  `1.0` 是这个 fork 自己版本线的起点，从这里开始独立维护（以后发新版改这
  一行 + 打同名 tag，跟其他已经有版本号的 ENum fork 是同一套约定）。
  `install.sh --enum-version` 现在会真的报出这个号，不再永远返回空。
